name: nasa-apollo-lm

object:
  kind: file
  path: models/nasa/apollo-lunar-module.glb
  center: true
  target_size: 4.0
  max_faces: 40000

flow:
  speed: 12.0
  direction: [1.0, 0.0, 0.0]

grid:
  resolution: 34
  padding: 0.8

viz:
  vector_density: 16
  interactive: true

output_dir: output
